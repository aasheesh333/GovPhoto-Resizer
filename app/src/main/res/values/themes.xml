<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for splash screen -->
    <style name="Theme.GovPhotoResizer.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.GovPhotoResizer</item>
    </style>

    <!-- Main app theme — default Light, Material3 theming handled in Compose -->
    <style name="Theme.GovPhotoResizer" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:statusBarColor">@color/surface_light</item>
        <item name="android:navigationBarColor">@color/surface_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="colorPrimary">@color/primary</item>
    </style>
</resources>
